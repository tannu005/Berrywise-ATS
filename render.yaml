services:
  - type: web
    name: recruitment-pipeline
    env: node
    plan: free
    rootDir: .
    buildCommand: npm run build
    startCommand: NODE_ENV=production npm start
    envVars:
      - key: PORT
        value: 5000

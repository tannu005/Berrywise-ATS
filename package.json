{
  "name": "recruitment-pipeline-optimizer",
  "version": "1.0.0",
  "description": "AI-Powered Full-Stack Candidate Evaluation Platform",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --legacy-peer-deps --prefix frontend",
    "build": "npm run install:all && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "start:backend": "npm run start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  }
}
